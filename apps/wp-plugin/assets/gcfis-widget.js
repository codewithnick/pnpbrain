/**
 * PNpbrain Widget — bundled embed script placeholder.
 *
 * In production this file is replaced by the output of:
 *   pnpm --filter @gcfis/widget run build:embed
 *
 * The build script (apps/widget/scripts/build-embed.mjs) compiles
 * apps/widget/embed.tsx → dist/gcfis-widget.js (IIFE, minified, ES2020)
 * and that output is then copied here by the wp-plugin build step.
 *
 * If this file is the placeholder (i.e. the bundle has not been built yet),
 * the plugin will still load but the widget will not render.
 */
