{
  "name": "@pnpbrain/web-sdk",
  "version": "0.0.1",
  "private": true,
  "description": "Browser chat SDK for PNpbrain (WebSocket with SSE fallback)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "type-check": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@pnpbrain/types": "workspace:*"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.7.3"
  }
}